name: Sandbox source export
on:
  pull_request:
    branches:
      - main
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: turtlepen-sandbox-source
          path: .
          include-hidden-files: true
          retention-days: 1
